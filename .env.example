# img-iter-agent 环境变量样例
# 复制为 .env 并填入真实值（.env 已被 .gitignore 忽略）。
# 前缀统一为 IMG_ITER_。

# --- dmxapi 凭证/地址（Step 3 真正生图时才需要；基础层测试无需配置）---
# 网关域名用 www.dmxapi.cn（api.dmxapi.cn 证书已失效/主机名不匹配，勿用）
IMG_ITER_DMXAPI_HOST=https://www.dmxapi.cn
IMG_ITER_DMXAPI_KEY=

# --- 四个协议族的「生图」model_id（dmxapi model_id；按族填）---
# Family B(豆包Responses): seedream-5.0-pro
IMG_ITER_MODEL_SEEDREAM_PRO=doubao-seedream-5-0-pro-260628
# Family A(OpenAI Images): gpt-image-2
IMG_ITER_MODEL_GPT_IMAGE=gpt-image-2-03
# Family D(Gemini native): gemini-3.1-flash-image（non-lite 支持 2K；lite 版被 Google 限 1K）
IMG_ITER_MODEL_GEMINI_IMAGE=gemini-3.1-flash-image
# Family C(Qwen Responses): qwen-image-2.0
IMG_ITER_MODEL_QWEN_IMAGE=qwen-image-2.0-pro

# --- Agent LLM（每个 agent 独立配置；见 ADR-006）---
# Critic 看图打分 → 必须多模态；Generator 文本即可；Summarizer(=经验蒸馏器) 也需多模态看图。
# 三个 agent 都走 dmxapi 的 OpenAI 兼容端点（/v1/chat/completions，需支持 tool-calling）。
IMG_ITER_CRITIC_MODEL=gemini-3.1-flash-lite
IMG_ITER_GENERATOR_MODEL=deepseek-v4-flash
IMG_ITER_SUMMARIZER_MODEL=gemini-3.1-flash-lite

# --- 数据根目录（默认 = 项目下 data/，一般无需改）---
# IMG_ITER_DATA_ROOT=

# --- LangSmith 追踪（agent 运转可视化，接入 graph studio）---
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=lsv2_pt_xxxxxxxx
LANGSMITH_PROJECT=img-iter-agent
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
